version: '3'

services:
  web:
    build: .
    container_name: nextjs-app
    ports:
      - "3000:3000"

  nginx:
    image: nginx:1.28.0
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - web
